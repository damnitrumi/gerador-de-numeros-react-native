/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';

import GeradorNumeros from './src/GeradorNumeros';

AppRegistry.registerComponent(appName, () => GeradorNumeros);
